# Writeo

A blogging platform built with Express, Prisma, and TypeScript (backend) + React frontend.

## Structure
- **/backend** → Express + Prisma + TypeScript REST API
- **/frontend** → React-based client (TypeScript)

## How to run
### Backend
```bash
cd backend
npm install
npm run dev
